#!/bin/sh
set -e
npm run check        # biome --write: auto-fix lint + format
npm run format:md    # prettier --write: auto-format markdown
git add -u           # re-stage any files auto-formatted above
npm run typecheck    # type errors can't be auto-fixed; fail here if any
