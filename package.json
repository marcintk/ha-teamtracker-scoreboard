{
  "name": "ha-teamtracker-scoreboard-card",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome lint src",
    "format": "biome format --write src",
    "check": "biome check --write src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "rollup": "^4.0.0",
    "vitest": "^2.0.0"
  }
}
