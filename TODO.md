# TODO — Open Issues

## Notes

### `>` truncation indicator in `last_play` — `src/widgets.ts:53`

The `>` after truncated `last_play` text is intentional (same UX pattern as tv network badge
truncation). An automated review flagged it as a stray character; it is not a bug.
